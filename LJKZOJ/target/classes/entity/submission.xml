<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="entity">
    <insert id="addSubmission" parameterType="Submission">
        insert into submissions(email, pid, submitTime, state, normalSubmit)
        values (${email}, ${pid}, ${submitTime}, ${state}, ${normalSubmit})
    </insert>

    <update id="updateState" parameterType="Submission">
        update submissions set state = ${state} where email = ${email} and
        pid = ${pid} and submitTime = ${submitTime}
    </update>

    <select id="getSubmissionList" resultType="Submission" parameterType="Map">
        select * from submissions limit ${start}, ${size}
    </select>
</mapper>