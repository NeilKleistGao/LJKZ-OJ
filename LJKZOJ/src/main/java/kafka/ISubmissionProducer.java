package kafka;

import entity.Submission;

import javax.ejb.Local;
import java.util.List;

@Local
public interface ISubmissionProducer {
//    void insert(entity.Submission submission);
//    void updateState(entity.Submission submission);
//    List<Submission> getSubmissionList(int start, int size);
}
